<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Homepage', root_path, class: 'nav-link' %>
        </li>

        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Contabilità', backoffice_counts_url, class: 'nav-link' %>
          </li>

          <%= button_to destroy_user_session_path, class: 'btn btn-secondary', :method => :delete do %>
            <i aria-hidden="true" class="fas fa-sign-out-alt mr-1 w-5"></i>
            <span class="nav-label">
              Log Out
            </span>
          <% end %>
        <% else %>
          <%= link_to new_user_session_path, class: 'btn btn-primary' do %>
            <i aria-hidden="true" class="fas fa-sign-in-alt mr-1 w-5"></i>
            <span class="nav-label">
              Log In
            </span>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
